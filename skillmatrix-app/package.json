{
  "name": "skill-matrix-app",
  "version": "2.0.0",
  "description": "Employee Skill Matrix Management System — Node.js + SQLite backend, REST API, and a frontend with optional Ollama-powered natural language search.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "seed": "node server/seed.js",
    "reset-db": "node -e \"require('fs').rmSync('server/data/skillmatrix.db',{force:true}); console.log('Database reset — will start empty on next run.');\""
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
